import { StyledHeader } from "./Header.styled";
import { Navigation } from "./Navigation/Navigation"

export const Header = () => {
  return (
    <StyledHeader>
      <Navigation />
    </StyledHeader>
  );
}